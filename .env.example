# Runtime catalog pointer. This repo does not publish schemas; it only displays
# the Filebase IPFS catalog produced outside the site.
# Prefer a stable IPNS name. A direct gateway URL also works.
LEXICON_MANIFEST_IPNS=
LEXICON_MANIFEST_URL=
